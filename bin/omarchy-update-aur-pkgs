#!/bin/bash

# omarchy:summary=No-op on Fedora Asahi — AUR packages are not available on RPM-based systems.

# AUR is Arch-specific. On Fedora Asahi, packages come from dnf/COPR only.
# This command intentionally does nothing and exits successfully so that
# omarchy-update does not break when running this step.
exit 0
