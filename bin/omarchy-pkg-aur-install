#!/bin/bash

# omarchy:summary=Not available on Fedora Asahi — AUR packages have no equivalent on RPM-based systems.
# omarchy:requires-sudo=true

echo "Error: AUR is not available on Fedora Asahi." >&2
echo "Use 'omarchy pkg install' to browse and install Fedora packages via dnf." >&2
exit 1
